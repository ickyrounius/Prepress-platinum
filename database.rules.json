{
  "rules": {
    "notifications": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        "$notificationId": {
          ".write": "auth != null && auth.uid === $uid",
          ".validate": "newData.hasChildren(['message', 'read', 'timestamp'])"
        }
      }
    },
    "locks": {
      ".read": "auth != null",
      "$jobId": {
        ".write": "auth != null && ((!data.exists() && newData.exists() && newData.child('activeUser/uid').val() === auth.uid) || (data.exists() && !newData.exists() && data.child('activeUser/uid').val() === auth.uid))",
        ".validate": "newData.hasChildren(['activeUser', 'timestamp']) && newData.child('activeUser/uid').isString() && newData.child('timestamp').isNumber()"
      }
    },
    "active_jobs": {
      ".read": "auth != null",
      "$collection": {
        "$jobId": {
          ".write": "auth != null && ((!data.exists() && newData.child('operator_id').val() === auth.uid) || (data.exists() && newData.exists() && newData.child('operator_id').val() === auth.uid) || (data.exists() && !newData.exists() && data.child('operator_id').val() === auth.uid))",
          ".validate": "newData.hasChildren(['id', 'operator_id']) && newData.child('operator_id').isString()"
        }
      }
    },
    "presence": {
      "$uid": {
        ".read": "auth != null",
        ".write": "auth != null && auth.uid === $uid",
        ".validate": "newData.hasChildren(['online', 'lastSeen'])"
      }
    }
  }
}
